<!DOCTYPE html>
<html>

<head>
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body>
    <header class="grid grid-cols-1 justify-items-center gap-5">
        <h1 class="text-6xl font-bold">
            <%= title %>
        </h1>
        <p>Welcome to <%= title %>
        </p>
    </header>
    <div id="messages-wrapper" class="grid grid-cols-1">
        <ul class="flex flex-col justify-center gap-5 text-center">
            <% messages.map((msg) => { %>
                <li class="single-msg-wrapper">
                    <div class="sent-by"><%=msg.user%></div>
                    <div class="msg-content"><%=msg.text%></div>
                    <div class="date-added"><%=format(msg.added)%></div>
                </li> 
            <% }) %>
        </ul>
        <a class="relative p-3 border-2 border-solid rounded justify-self-center mt-5 max-w-fit" href="/new">Add a message</a>
    </div>
</body>

</html>